[pytest]
# Disable pytest-cov auto-load because its coverage dependency imports sqlite3,
# which fails with the current Python build on this machine.
addopts = -p no:cov
markers =
    integration: slow/integration tests that may call external tools
